name: "Deadline Cloud Client Build Installer"

on:
  workflow_dispatch:
    inputs:
      os:
        required: true
        default: "linux"
        type: choice
        options:
        - Linux
        - Windows
        - MacOS
      ref_type:
        required: true
        type: choice
        options:
        - heads
        - tags
      ref:
        required: true
        type: string
        default: "mainline"
      environment:
        required: true
        type: choice
        options:
        - mainline
        - release

jobs:
  BuildDeadlineCloudClientInstaller:
    if: (github.repository == 'justinsaws/deadline-cloud')
    name: Build Deadline Cloud Client Installer
    uses: justinsaws/.github/.github/workflows/reusable_build_installers.yml@mainline
    secrets: inherit
    permissions:
      contents: read
      id-token: write
    secrets: inherit
    with:
      environment: ${{inputs.environment}}
      ref_type: ${{inputs.ref_type}}
      ref: ${{inputs.ref}}
      oses: "['${{inputs.os}}']"
      project_name: "deadline-cloud"
